# =====================================================================
# Public values — safe to expose in the browser (RLS protects everything)
# =====================================================================
NEXT_PUBLIC_SUPABASE_URL=https://jcjygxooykoyhkbuoxex.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_enc7sZVW8c07j0N4LBIpIA_mG6tlzje

# =====================================================================
# Server-only secrets — NEVER commit, NEVER hardcode in browser JS
# =====================================================================
#
# When deployed to NETLIFY (current production target):
#   Set these in:  Netlify project → Site configuration → Environment variables
#   They are read by netlify/functions/chat.mts at request time.
#
# OPENROUTER_API_KEY=sk-or-v1-...
# OPENROUTER_MODEL=openrouter/free
# ALLOWED_ORIGIN=https://chaclacayo.netlify.app
#
# When deployed to SUPABASE Edge Functions (alternative path):
#   Set via:  supabase secrets set OPENROUTER_API_KEY=sk-or-v1-...
# OPENROUTER_API_KEY=sk-or-v1-...
# OPENROUTER_MODEL=openrouter/free
# ALLOWED_ORIGIN=https://chaclacayo.netlify.app,https://www.chaclacayo.example.com
